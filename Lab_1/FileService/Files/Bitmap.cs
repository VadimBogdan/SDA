namespace FileService.Files
{
    public class Bitmap : CustomFile
    {
        public Bitmap(string fileName, string fileExtension, long timeCreation)
            : base(fileName, fileExtension, timeCreation) { }
    }
}
